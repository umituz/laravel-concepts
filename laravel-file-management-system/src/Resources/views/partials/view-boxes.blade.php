<button class="btn tip{{ $view == 0 ? ' btn-inverse' : null }}" id="view0"
        data-value="0"
        title="{{ __('View Boxes') }}">
	<i class="icon-th {{ $view == 0 ? 'icon-white' : null }}"></i>
</button>