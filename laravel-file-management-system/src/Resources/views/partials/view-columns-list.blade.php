<button class="btn tip{{ $view == 2 ? ' btn-inverse' : null }}" id="view2"
        data-value="2"
        title="{{ __('View Columns List') }}">
	<i class="icon-fire {{ $view == 2 ? 'icon-white' : null }}"></i>
</button>